package ui;

// Creates an instance of the OrderTracker class and starts the application by calling the start method.
public class Main {
    public static void main(String[] args) {
        OrderTracker orderTracker = new OrderTracker();
        orderTracker.start();
    }

}